import React from 'react'
import classNames from 'classnames'

const Footer = ({ className }) => {
  return <footer className={classNames(['base-footer', className])}></footer>
}

export default Footer
